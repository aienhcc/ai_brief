# 주간 AI 브리핑 — 자동 실행 지시서

이 파일은 **매주 월요일 아침에 자동으로 실행되는 클라우드 에이전트가 읽는 지시서**입니다.
브리핑의 형식이나 규칙을 바꾸고 싶으면 **이 파일만 고치면** 다음 주부터 바뀝니다.

---

## 독자

너는 지금 **비개발자 문과생 독자 한 명**을 위한 주간 브리핑을 만든다. 이 사람은
HTML/CSS/JS 프론트엔드를 AI에게 시켜서 만들고, GitHub로 배포하는 정도는 따라 할 수 있다.
백엔드·터미널·프로그래밍 개념은 모른다. 하지만 관심은 아주 많고, 겁먹지 않는다.

## 1단계 — 지난 호 확인 (먼저 할 것)

`ai-brief/` 폴더의 파일 목록을 보고, **가장 최근 2개 호를 읽는다.**
이미 다룬 주제는 반복하지 않는다. 단, 지난 호에 다룬 게 크게 바뀌었으면
"지난 호 그거, 이렇게 됐습니다"로 짧게 업데이트한다.

기초 개념은 `ai-foundations.html`(총론)에 이미 정리돼 있다. 총론에 있는 용어는
다시 설명하지 말고, 새 용어가 나오면 **총론의 여섯 묶음(재료·대화·지식·행동·조직·신뢰)
중 어디에 해당하는지**를 한 마디로 짚어줘라. 기초를 반복하지 말고 변화만 다뤄라.

## 2단계 — 조사

`WebSearch`로 **최근 1주 이내**에 실제로 화제가 된 것을 찾는다. 최소 4~6번 검색하고,
서로 다른 각도로 훑어라. 예시 각도:

- Claude Code / Codex / Cursor 등 에이전트 도구의 새 기능 (공식 체인지로그를 직접 확인할 것)
- 새로 뜬 스킬 · 플러그인 · 슬래시 커맨드
- 사람들이 새로 만들어낸 작업 방식 (워크플로 패턴)
- 비개발자 / 바이브코딩 쪽에서 실제로 쓰이는 도구
- 모델 자체의 변화 (새 모델, 가격, 컨텍스트)

**중요 — 유행과 대세를 구분해라.** 한 사람이 트윗해서 잠깐 뜬 것과, 여러 곳에서
반복적으로 언급되며 실제 사용 흔적(설치 수, 여러 독립 출처)이 있는 것은 다르다.
근거가 약하면 그렇게 밝히고, 확신이 없으면 아예 빼라. **없는 걸 지어내지 마라.**

## 3단계 — 고르기

**5개 이하로 추린다.** 많이 담는 게 목적이 아니다. 아래 기준으로 순서를 매긴다:

1. 이 독자가 **이번 주에 실제로 써볼 수 있는가**
2. 없으면, **알아두면 대화가 통하는가** (용어 이해)
3. 나머지는 버린다

각 항목마다 반드시:
- **이게 뭐냐** — 비유 하나로 한 문장. 전문용어를 쓰면 반드시 괄호로 풀어쓴다.
- **왜 떴냐** — 어떤 불편을 해결해서 떴는지. 근거(출처, 날짜, 버전 등)를 붙인다.
- **나한테 쓸모 있나** — `지금 바로` / `나중에` / `알기만` 중 하나로 솔직하게.
  대부분은 `알기만`이다. 전부 `지금 바로`면 그건 거짓말이다.
- **예시** — 그대로 복사해서 붙여넣을 수 있는 프롬프트나 명령. 추상적인 설명 금지.

조사해봤는데 이번 주엔 별거 없으면, **억지로 5개를 채우지 말고 2개만 쓰고
조용한 주였다고 말해라.**

## 4단계 — 리포트 파일 만들기

- 저장 위치: `ai-brief/YYYY-MM-DD.html` (오늘 날짜. `date +%Y-%m-%d`로 확인)
- 페이지 제목(`<title>`): `AI 브리핑 YYYY.MM.DD`
- **디자인은 가장 최근 호 파일의 `<style>` 블록을 그대로 복사해서 쓴다.**
  매주 같은 디자인이어야 시리즈로 읽힌다. 새로 디자인하지 마라.
- 맨 위에 **이번 주 한 줄 요약**과 **이번 주에 딱 하나만 해볼 것**을 놓는다.
  독자가 스크롤을 안 해도 이 두 개는 보이게.
- 본문은 위에서 고른 항목들.
- 맨 아래에 **출처 링크 목록**. 링크 없는 주장은 쓰지 마라.
- 호수(1호, 2호…)는 `ai-brief/` 폴더의 파일 개수 + 1 로 매긴다.

## 5단계 — 목록 페이지 갱신

`index.html`의 목록에 이번 호를 **맨 위에** 한 줄 추가한다.
기존 항목의 형식을 그대로 따라 한다.

## 6단계 — GitHub에 저장

```
git add -A
git commit -m "브리핑 N호 (YYYY-MM-DD)"
git push
```

커밋 메시지는 한 줄이면 된다.

## 7단계 — 카카오톡으로 알림

`KakaotalkChat-MemoChat` 도구로 **나에게 보내기**를 써서 아래 형식으로 보낸다.
**딱 이 형식만. 길게 쓰지 마라.**

```
[AI 브리핑 N호]
1. (이번 주 핵심 한 줄)
2. 할 일: (이번 주에 해볼 것 하나)
3. https://aienhcc.github.io/ai_brief/ai-brief/YYYY-MM-DD.html
```

카카오톡 전송이 실패하면 **거기서 멈추지 말고** 6단계까지는 이미 끝났으니
실패 사실만 마지막 응답에 남겨라. 리포트 자체는 GitHub에 남아 있으면 성공이다.

## 지켜야 할 것

- 한국어로 쓴다. 영어 용어는 처음 나올 때 한 번 풀어준다.
- "혁명적", "게임체인저" 같은 말을 쓰지 마라. 뭘 할 수 있게 되는지를 구체적으로 써라.
- 날짜·버전·숫자는 **출처에서 확인한 것만** 쓴다. 기억으로 쓰지 마라.
